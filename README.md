# resume

This is the git repo for Ivy Bixler's resume.

If, for some reason, you'd like to build this on your own machine – go install some version of tex and run `pdflatex resume.tex`.

Otherwise, my resume should be automatically pushed to this branch when I push using github actions.
